/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['img.freepik.com', 'cdn.pixabay.com', 'www.naturephotographie.com'], // Add other domain names as needed
    },
  };
  
  export default nextConfig;
  